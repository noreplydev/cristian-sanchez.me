import React from 'react'

// STYLED COMPONENTS
import { P } from './style.js'

export const CardMenu = ({ projectName }) => {
  return (
    <div>
        <P>{projectName}</P>
        <div>
        </div>
    </div>
  )
}
